# Cordverse Configuration Example

# The port the unified server will run on (Default 80 for production)
PORT=80

# A secure master password to lock the entire application interface.
# REQUIRED for security.
CORDVERSE_PASSWORD=your_secure_master_password_here

# A 32-character random string used for encrypting Discord tokens at rest.
# Keep this secret! If you lose it, you lose access to saved accounts.
ENCRYPTION_KEY=your_random_32_character_encryption_key_here

# Optional: Path to the SQLite database
# DATABASE_PATH=./data/cordverse.db
